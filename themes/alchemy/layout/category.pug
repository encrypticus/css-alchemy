extends partials/layout

block content
  .page__content-inner
    section.page__posts
      include blocks/post-list/post-list
      if page.total > 1
        +pagination

    include blocks/sidebar/sidebar